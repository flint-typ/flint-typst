# Upstream clone — the reference implementation the tests compare against and
# the source of the fixture corpus. Not vendored; recreate with `make upstream`.
flint-source/

# Python venv for the test harness (`make setup`)
.venv/

# Test and bench scratch
tests/_case.json
tests/_cases.json
tests/typst-probes/_*.typ
bench/_*.typ
*.pdf
__pycache__/
